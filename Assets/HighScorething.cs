using UnityEngine;

public class HighScorething : MonoBehaviour
{

    //I got up to around the high score before I kinda ran out of time.. everything is a bit JaNKY
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
